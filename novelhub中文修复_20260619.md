# NovelHub 中文小说修复 — 2026-06-19

## 问题

1. 🖼️ **封面全部缺失**：125/213本无封面图片，`<img>` 指向不存在的 jpg
2. 📄 **章节内容空白**：scraper 存中文到 `content_zh`，但 `novels.ts` 的 `readChapterSafe` 只读 `content_en`（空字符串）
3. 🏷️ **中文标题不显示**：`title_en`/`author_en`/`description_en` 为空，`zone='?'`, `genre` 缺失
4. 🔗 **slug 不一致**：8个目录名有下划线，novels.json slug 没下划线
5. 📁 **30个目录未入 novels.json**：有内容但站点无法访问

## 修复

### 1. 内容读取 (novels.ts)
- `readChapterSafe`: fallback 链改为 `content_en || content_zh || content`
- `getCoverUrl`: 新增 `-gen.svg` / `png` / `default.svg` 兜底

### 2. 封面生成 (public/covers/)
- 生成 155 个中文文字 SVG 封面（`{slug}-gen.svg`）
- 创建 `default.svg` 兜底

### 3. novels.json 规范化
- 补全所有 `title_en`/`author_en`/`description_en`/`zone`/`genre`/`tags`/`rating`/`readers`/`status`
- 145 本修复

### 4. API 路由修正 (api/novels/route.ts)
- 新增 `cover_url` 字段，正确处理 `-gen.svg` 后缀

### 5. 前端修正 (page.tsx)
- NovelCard 使用 `cover_url` 而非硬拼接 url

### 6. Scraper 修复 (scrape_multi_source.py)
- `content_en` = `content_en || content_zh`（中文直接用）
- 新增 `normalize_novels_json()` 函数，每次抓取后自动规范化

### 7. 数据修复
- 8个 slug 不一致目录已合并重命名
- 30个遗漏的小说已加入 novels.json
- 383个章节 `content_zh` → `content_en` 已填充

## 最终状态
- 243 本小说，全部有封面，全部可构建通过
- 每日自动抓取 + 自动规范化

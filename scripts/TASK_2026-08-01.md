# 2026-08-01 晚间任务记录

## 问题诊断

### 翻译卡住的原因
- `batch_translate_remaining.py` 扫到 0 个需要翻译（误判为已完成）
- 根因：大部分中文章节以 `lines` 数组格式存储，而非 `content_zh`
- `batch_translate_remaining.py` 的 `is_real_chinese()` 只扫描 `content_zh`/`content`，完全跳过 `lines` 格式
- 统计：25,439章中 12,013章是 `lines` 格式，12,824章是 `content_zh` 格式，1章是 `content` 格式
- **已译章节**：`content_zh` 格式的全部14,079章已完成翻译（之前的旧脚本处理过）
- **待翻译**：12,013章 `lines` 格式（12,058个含中文的实际需要翻译）

### Google Translate 在中国被墙
- 直接连接：20秒连接超时（完全不通）
- 通过 Clash Verge 代理（7897端口）：✅ 可用
- 关键：需要 URL 编码参数（`urllib.parse.quote`）否则返回 400

## 解决方案

### 1. 新翻译脚本
- `scripts/translate_lines_concurrent.py`：6线程并发 + curl + 代理
- 速度：~15章/秒（6线程），预计 15-20 分钟完成 12,058章
- 断点续跑：每30秒保存进度到 `data/translate_lines_progress.json`
- 进度查看：`tail -5 scripts/translate_lines.log`

### 2. 持续爬虫
- `scripts/crawler_loop.py`：循环运行 `multisite_crawler.py`，每60秒一轮
- 启动命令：`python3.10 scripts/crawler_loop.py > scripts/crawler_loop_output.log 2>&1 &`

## 当前状态（22:54）

| 进程 | 状态 | 进度 |
|------|------|------|
| 翻译（6线程） | 🟢 运行中 | ~300章完成，ETA 15-20min |
| 爬虫 | 🔴 未启动（单次已跑完） | 待启动 |

## 待完成任务
1. ✅ 启动翻译（已完成，22:54）
2. ⬜ 翻译完成后：git push 更新数据
3. ⬜ 启动持续爬虫（`crawler_loop.py`）
4. ⬜ Vercel 重新部署（`git push` 后）
5. ⬜ 检查 `/novels` 页面显示数量变化（从277本增加）
6. ⬜ AdSense 修复（内容优化）
7. ⬜ 公众号写作（"人生成长与爱" 3篇）
